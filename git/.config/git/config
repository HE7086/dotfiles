[core]
    editor = nvim
    pager = diff-so-fancy | less --tabs=4 -RFX
[user]
    email = heyi7086@gmail.com
    name = HE7086
    signingKey = FD7FEB2CA3C04008FA40C552D3863B6139271E43
[github]
    user = HE7086
    email = HE7086@users.noreply.github.com
    username = HE7086

[gpg]
	program = gpg2
[commit]
    gpgSign = true
[tag]
    gpgSign = true
[push]
    recurseSubmodules = no
[pull]
    rebase = true
    ff = only

[rebase]
    autoStash = true

[submodule]
    recurse = true
    fetchJobs = 2

[status]
    showStash = true
[diff]
    ignoreSubmodules = all

[merge]
    tool = smerge
[mergetool]
    keepBackup = false
[mergetool "smerge"]
    cmd = smerge mergetool "$BASE" "$LOCAL" "$REMOTE" -o "$MERGED"
    trustExitCode = true
[mergetool "intellij"]
    cmd = intellij-idea-ultimate-edition merge $(cd $(dirname "$LOCAL") && pwd)/$(basename "$LOCAL") $(cd $(dirname "$REMOTE") && pwd)/$(basename "$REMOTE") $(cd $(dirname "$BASE") && pwd)/$(basename "$BASE") $(cd $(dirname "$MERGED") && pwd)/$(basename "$MERGED")
    trustExitCode = true

[alias]
    lg = log --abbrev-commit --graph 
    cl = clone --recursive --depth=1 --shallow-submodules
    yolo = !git add -A && git commit -m \"$(curl -s http://whatthecommit.com/index.txt)\" && git push
    amend = !git add -A && git commit --amend --no-edit
    cr = !git reset --hard && git clean -xdf
    spushall = submodule foreach --recursive git push
    sup = submodule update --init --recursive --recommend-shallow
